/**
 * @class Essentials
 * @classdesc Representa los datos esenciales del juego.
 * @interface
 */
export interface Essentials {
    gameSize: number;
    gameType: string;
    gameMode: string;
    gameDifficulty: string;
}